<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.getswitchpal.android"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="18"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <!-- for QR Code scan -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.getswitchpal.android.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.getswitchpal.android.ScanActivity" />
        <activity android:name="com.getswitchpal.android.DeviceActivity" />

        <activity android:name="com.getswitchpal.android.DeviceControlActivity" />
        <activity android:name="com.getswitchpal.android.DeviceScanActivity" />

        <service
            android:name="com.getswitchpal.android.BluetoothLeService"
            android:enabled="true" />

    </application>

</manifest>
